//
// <%= name %> unit tests
//
define([
  'modules/<%= name %>/main'
], function(<%= name %>) {
  describe("<%= name %>", function() {
    it("should work", function() {
      expect(true).to.equal(true);
    });
  });
});
